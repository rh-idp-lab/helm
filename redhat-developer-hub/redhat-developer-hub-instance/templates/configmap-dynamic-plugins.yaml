apiVersion: v1
kind: ConfigMap
metadata:
  name: dynamic-plugins-rhdh
  namespace: {{ .Values.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
data:
  "dynamic-plugins.yaml": |
    includes:
      - dynamic-plugins.default.yaml
    plugins:

      # ── Kubernetes / OpenShift Topology ──────────────────────────────────────
      # Chapter 2 (Red Hat supported) + Chapter 3 (Tech Preview) — bundled
      # Ref: RHDH 1.9 dynamic plugins reference
      - package: ./dynamic-plugins/dist/backstage-plugin-kubernetes-backend-dynamic
        disabled: false
      - package: ./dynamic-plugins/dist/backstage-plugin-kubernetes
        disabled: false
      - package: ./dynamic-plugins/dist/backstage-community-plugin-topology
        disabled: false

      # ── OpenShift Pipelines (Tekton) ─────────────────────────────────────────
      # Chapter 2 (Red Hat supported) — bundled, v3.33.3
      - package: ./dynamic-plugins/dist/backstage-community-plugin-tekton
        disabled: false

      # ── ArgoCD ───────────────────────────────────────────────────────────────
      # Chapter 2 (Red Hat supported) — bundled, v4.6.0
      - package: ./dynamic-plugins/dist/roadiehq-backstage-plugin-argo-cd-backend
        disabled: false
      # Chapter 3 (Tech Preview) — bundled, v1.8.1
      - package: ./dynamic-plugins/dist/roadiehq-scaffolder-backend-argocd-dynamic
        disabled: false

      # ── GitLab ───────────────────────────────────────────────────────────────
      # Chapter 2 (Red Hat supported) + Chapter 3 (Tech Preview) — bundled
      - package: ./dynamic-plugins/dist/backstage-plugin-catalog-backend-module-gitlab-dynamic
        disabled: false
      - package: ./dynamic-plugins/dist/backstage-plugin-catalog-backend-module-gitlab-org-dynamic
        disabled: false
      - package: ./dynamic-plugins/dist/backstage-plugin-scaffolder-backend-module-gitlab-dynamic
        disabled: false
      # Chapter 4.4 (community migrated to ghcr.io in RHDH 1.9) — v6.13.0
      # Tag format: bs_<backstage-version>__<plugin-version> (RHDH 1.9 = Backstage 1.45.3)
      - package: 'oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/immobiliarelabs-backstage-plugin-gitlab-backend:bs_1.45.3__6.13.0'
        disabled: false

      # ── Keycloak ─────────────────────────────────────────────────────────────
      # Chapter 2 (Red Hat supported) — bundled, v3.16.0
      - package: ./dynamic-plugins/dist/backstage-community-plugin-catalog-backend-module-keycloak-dynamic
        disabled: false

      # ── Quay ─────────────────────────────────────────────────────────────────
      # Chapter 2 (Red Hat supported) — bundled, v1.28.1
      - package: ./dynamic-plugins/dist/backstage-community-plugin-quay
        disabled: false
      # Chapter 4.4 (community migrated to ghcr.io in RHDH 1.9) — v1.10.1
      - package: 'oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-community-plugin-quay-backend:bs_1.45.3__1.10.1'
        disabled: false

      # ── RBAC ─────────────────────────────────────────────────────────────────
      # Chapter 2 (Red Hat supported) — bundled, v1.47.4
      - package: ./dynamic-plugins/dist/backstage-community-plugin-rbac
        disabled: false
